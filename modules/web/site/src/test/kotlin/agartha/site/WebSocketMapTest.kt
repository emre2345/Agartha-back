package agartha.site

import org.assertj.core.api.Assertions.assertThat
import org.junit.Test

class WebSocketMapTest{
    @Test
    fun webSocketHashMap_connect_newKeyAndValue() {
        WebSocketHandler().
        assertThat(201).isEqualTo(200)
    }
}